# Core Dependencies
openai>=1.0.0                # Azure OpenAI API client
requests>=2.31.0             # HTTP client for API calls (includes urllib3, certifi)
Pillow>=10.3.0               # Image processing for hero image generation
beautifulsoup4>=4.12.0       # HTML parsing for newsletter extraction

# Azure Services
azure-storage-blob>=12.19.0  # Azure Blob Storage client for newsletter uploads
azure-identity>=1.15.0       # Azure authentication with DefaultAzureCredential

# Security & Code Quality
bandit[toml]>=1.7.5          # Security linting for Python code
pre-commit>=3.5.0            # Git hook management for automated checks
black>=24.1.0                # Python code formatter
isort>=5.13.0                # Python import sorter
flake8>=7.0.0                # Python linter (ARM-compatible)
mypy>=1.13.0                 # Python type checker
